# Mosquitto MQTT Broker configuration for pet tracking system

# Basic configuration
pid_file /tmp/mosquitto.pid
persistence true
persistence_location /tmp/mosquitto/

# Allow anonymous access for local testing
allow_anonymous true

# Logging configuration
log_dest stdout
log_type all
connection_messages true

# Listen on all interfaces
listener 1883
